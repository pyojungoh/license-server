# Android Studio 프로젝트에 파일 복사하기

## 📁 복사해야 할 파일들

### 1단계: Java/Kotlin 소스 파일 복사

#### 복사 위치
Android Studio 프로젝트의 `app/src/main/java/com/` 폴더 아래에:

```
com/
└── hanjin/
    ├── Config.kt
    ├── LoginActivity.kt
    ├── MainActivity.kt
    ├── api/
    │   └── ApiService.kt
    ├── ble/
    │   └── BLEService.kt
    ├── models/
    │   ├── LoginRequest.kt
    │   └── LoginResponse.kt
    └── utils/
        └── DeviceUtils.kt
```

#### 복사 방법
1. Android Studio 왼쪽 파일 트리에서 `app/src/main/java/com/hanjin/automation/` 폴더 찾기
2. `automation` 폴더를 오른쪽 클릭 → Delete (또는 이름을 `hanjin`으로 변경)
3. `C:\hanjin\mobile_app\android\app\src\main\java\com\hanjin\` 폴더의 모든 파일 복사
4. Android Studio 프로젝트의 `app/src/main/java/com/hanjin/` 폴더에 붙여넣기

### 2단계: 리소스 파일 복사

#### 복사 위치
`app/src/main/res/` 폴더 아래:

```
res/
├── layout/
│   ├── activity_login.xml
│   └── activity_main.xml
└── values/
    └── strings.xml
```

#### 복사 방법
1. `C:\hanjin\mobile_app\android\app\src\main\res\` 폴더 내용 복사
2. Android Studio 프로젝트의 `app/src/main/res/` 폴더에 붙여넣기 (덮어쓰기)

### 3단계: 설정 파일 복사

#### 복사할 파일들
1. **AndroidManifest.xml**
   - 복사: `C:\hanjin\mobile_app\android\app\src\main\AndroidManifest.xml`
   - 붙여넣기: `app/src/main/AndroidManifest.xml` (덮어쓰기)

2. **app/build.gradle.kts**
   - 복사: `C:\hanjin\mobile_app\android\app\build.gradle.kts`
   - 붙여넣기: `app/build.gradle.kts` (덮어쓰기)

3. **build.gradle.kts** (프로젝트 루트)
   - 복사: `C:\hanjin\mobile_app\android\build.gradle.kts`
   - 붙여넣기: 프로젝트 루트의 `build.gradle.kts` (덮어쓰기)

4. **settings.gradle.kts**
   - 복사: `C:\hanjin\mobile_app\android\settings.gradle.kts`
   - 붙여넣기: 프로젝트 루트의 `settings.gradle.kts` (덮어쓰기)

## ⚠️ 중요: 패키지 이름 확인

Android Studio에서 생성한 기본 패키지는 `com.hanjin.automation`일 수 있습니다.
우리가 만든 파일들은 `com.hanjin` 패키지를 사용합니다.

### 해결 방법
1. 파일 복사 후 Android Studio에서 각 파일을 열기
2. 파일 맨 위의 `package` 선언 확인
3. `package com.hanjin.automation` → `package com.hanjin`으로 변경
   - 또는 반대로 우리 파일들의 패키지를 `com.hanjin.automation`으로 변경

## 🔧 복사 후 해야 할 일

1. **Gradle Sync**
   - 상단 메뉴: File → Sync Project with Gradle Files
   - 또는 상단 알림바에서 "Sync Now" 클릭

2. **빌드 시도**
   - 상단 메뉴: Build → Make Project
   - 오류가 있으면 확인 및 수정

3. **패키지 이름 통일**
   - 모든 파일의 패키지 선언이 동일한지 확인





